#pragma once

#include "SimpleMath.h"

using namespace DirectX::SimpleMath;

namespace Defines
{
	const float g = 0.0000981f;

	//RETHINK THIS
	const Vector3 groundPlaneOrigin = Vector3(0.0f, 0.0f, 0.0f);
	const Vector3 groundPlane = Vector3(1.0f, 1.0f, 0.0f);
};
